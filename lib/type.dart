class Exam {
  String id;
  String name;
  Exam(this.id, this.name);
}

class ScoreData {
  String studentId;
  int score;
  bool approved = false;
  String id;
  ScoreData(this.studentId, this.score, this.approved, this.id);
}
